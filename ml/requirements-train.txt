# Optional: LoRA fine-tuning for empathetic support (install in a separate venv if you like)
# pip install -r requirements.txt -r requirements-train.txt
torch>=2.1.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.27.0
peft>=0.11.0
trl>=1.0.0
bitsandbytes>=0.43.0; platform_system != "Darwin"
